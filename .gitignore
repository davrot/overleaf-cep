# docker image build
.dockerignore

# user defined files
.env
docker-compose.override.yml

# local-only agent docs + plan notes (never track: they hold credentials/creds refs)
references/
AGENTS.md
todo.md

# Dependencies (npm workspaces: hoisted to root + per-workspace)
node_modules/

# Local dev tool state (pi agent)
.pi/

# Yarn 4 local cache (enableGlobalCache=false)
.yarn/
!/.yarn/releases
!/.yarn/patches
