{
  "id": "5792d664",
  "title": "Phase 3: editor chat/menu UX (items 10,11,12,13,14)",
  "tags": [
    "llm",
    "frontend",
    "ux"
  ],
  "status": "complete",
  "created_at": "2026-08-15T15:49:54.829Z"
}

## Items done (all lint-clean)
- **Item 10 (context-aware Ask AI)**: verified existing — free-form chat anchors selection as `<selected-text lines="a–b">` with char count (toolbar L190-205, L560-572); admin system+action prompts applied server-side.
- **Item 12 (Fix formula syntax)**: verified existing — toolbar menu "Fix formula syntax" (mode 11, kind 'mathfix'), `mathFix` template in LLMPrompts, and admin-editable `mathFix` key added to the admin action-template list this pass.
- **Item 13 (title/abstract/document generators)**: ADDED menu items "Generate title (full document)" / "Generate abstract (full document)"; `basisText` in postToAPI feeds the whole editor doc for modes 9/10 (selection fallback) — document-level generators as the reviewer wanted. Note: uses the current editor file doc (Overleaf per-file CodeMirror); multi-file aggregation would need a backend endpoint (follow-up if wanted).
- **Item 14 (emojis / design)**: removed ✏️ (toolbar Edit toggle), ⚠️/❌ (use-llm-chat status content). Pre-existing `console.warn/error` in use-llm-chat (no-console violations) replaced by UI-state signaling (ESLint now clean). Full design-token/scss unification of the inline-styled panel = follow-up.

LINT: toolbar + use-llm-chat ESLint-clean.
