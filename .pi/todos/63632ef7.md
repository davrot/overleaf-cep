{
  "id": "63632ef7",
  "title": "Phase 2: settings UI consolidation (items 1,2,3,8)",
  "tags": [
    "llm",
    "frontend",
    "settings"
  ],
  "status": "complete",
  "created_at": "2026-08-15T15:49:54.830Z"
}

## Items done (all lint-clean + reviewer-verified)
- **Item 2 (user panel discoverability)**: FINAL ARCHITECTURE = `features/settings/components/llm-user-section.tsx` (new, single-bundle) renders a gated "AI assistant" card linking to the LLM settings page; `/user/llm-settings` page still renders (restored). Rationale: modules/*/frontend are separate webpack entries — importing module React into the core bundle risks dual React instances, so embedding was rejected in favor of a safe in-settings card. `GET /user/llm-settings/json` endpoint added (key masked) for programmatic access.
- **Item 3 (admin in Manage Site group)**: LLM entry lives in the admin "Manage Site" nav dropdown alongside Users/Projects (`/admin/llm/settings`); `/admin` = ServerAdmin dashboard + per-tool pages pattern (Users/Projects are also standalone pages) — matches the fork's existing admin pattern, no core surgery needed.
- **Item 7 (check = model-list fetch)**: `checkLLMConnection` calls `provider.listModels()` (60s cap) and returns the list; admin page adopts `resp.models` into `knownModels` on test success (single round trip).
- **Item 8 (full list + checked subset)**: new `knownModels` state (pug meta `ol-knownModels`) on admin page; `allModels = known ∪ available ∪ allowed`; save body sends `knownModels`; unchecked models stay visible after reload; `mathFix` added to the admin action-template key list.
- **Item 1 (consistency)**: user section reuses OL components + module UI; full 4-component shared extraction still a follow-up (admin page keeps its own inline implementation).
LINT: all files ESLint-clean; REVIEWER: meta keys 21/21 match, gating verified (ExpressLocals llmAllowUserSettings → root.tsx).

## Follow-up (deferred to report)
- Extract shared LLM settings components into `features/settings/shared/llm/` + scss — admin page refactor (needs dual-React-safety review first).
