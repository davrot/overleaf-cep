{
  "id": "83f7b753",
  "title": "F20: materialize legacy row into llmProviders (user-reported 401 root cause)",
  "tags": [
    "llm",
    "fix",
    "user-reported"
  ],
  "status": "done",
  "created_at": "2026-08-21T00:18:03.563Z"
}

davrot's "Imported settings" row exists ONLY in legacy user fields (llmApiUrl/llmApiKey/llmModelName); llmProviders is empty. check/scan only read llmProviders rows → Test/Scan run keyless → provider 401. Fix: on first loadProviders(), persist legacy row into llmProviders[0] (encrypt key via LLMCrypto) and keep the 'legacy' id mapping. Add regression test.
