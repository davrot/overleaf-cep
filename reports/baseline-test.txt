# Test baseline (2026-08-15, pre-fix)

## Per-area results (read-only baseline run)

| Area | Result | Detail |
|---|---|---|
| services/datamanipulator | ✅ 4/4 pass | 1 file |
| services/dropboxinterface | ✅ 15/15 pass | 2 files |
| services/webdavinterface | ✅ 12/12 pass | 2 files |
| services/web/modules/webdav | ❌ **24 failed / 15 passed** | 4 files failed, 2 passed (see below) |
| services/web/modules/dropbox | — **no test files exist** | "No test files found" |

## webdav module: all 24 failures are TEST-SIDE bugs (app code compiles fine)

1. `test/unit/src/ConflictResolver.test.mjs` (14 fail): `const ConflictResolver = await import('./../../../app/src/ConflictResolver.mjs')` grabs the **module namespace**, not the default export → `ConflictResolver.resolve/calculateHash/...` are "not a function". Fix: `.default` (or static import). Confirmed the real module exports all of them (app/src/ConflictResolver.mjs:164-215).
2. `test/unit/src/WebdavTokenManager.test.mjs` (4 fail): same namespace bug → `WebdavTokenManager.saveUserCredentials/getUserCredentials is not a function`; also the file has lint no-undef (`encrypt`/`decrypt`) + imports `mongodb-legacy` (not in deps).
3. `test/integration/src/WebdavRoutes.test.mjs` (2 fail): wrong relative import path (`./../../app/src/WebdavPaths.mjs` resolves outside the module → "Cannot find module '/test/app/src/WebdavPaths.mjs'").
4. `test/unit/src/WebdavClient.test.mjs` (4 of 6 fail): tests make **real network calls** to `https://cloud.example/...` (ENOTFOUND) — no fetch mock; they only pass in an environment that can resolve the name (they should not).
5. Passing: `SyncStateManager.test.mjs`, `WebDAVServiceClient.test.mjs`.

## Consequence for the fix phase

- The AGENTS.md lint gate and the "vitest green" expectation must include repairing these 4 broken test files (test-side only) — tracked as part of **Batch -1** alongside the 59 baseline lint errors (see [baseline-lint.txt](baseline-lint.txt)).
- The dropbox module has zero tests; test additions are already part of Batches 0-2 (ARC-09).
